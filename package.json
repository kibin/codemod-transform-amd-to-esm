{
  "name": "babel-plugin-transform-amd-to-esm",
  "version": "0.0.5",
  "description": "",
  "main": "./build/index.js",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --config=jest.config.js",
    "tdd": "NODE_OPTIONS=--experimental-vm-modules jest --config=jest.config.js --watch",
    "clean": "rm -rf build",
    "build": "esbuild src/index.js --bundle --outfile=build/index.js --minify",
    "prepare": "npm run test && npm run clean && npm run build"
  },
  "author": "Alisa Kibin",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.22.9",
    "@babel/generator": "^7.22.9",
    "@babel/preset-env": "^7.22.9",
    "@babel/template": "^7.22.5",
    "babel-plugin-tester": "^11.0.4",
    "esbuild": "0.18.17",
    "jest": "^29.6.1"
  }
}
